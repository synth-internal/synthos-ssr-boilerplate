# Dependencies are reinstalled per build — never commit them.
node_modules/

# Build output is produced by Netlify on every push — NEVER commit it (unlike
# the static boilerplate, this project has no committed dist/).
dist/
.output/
.nitro/
.tanstack/
.netlify/
.vinxi/

# Generated by the TanStack Router plugin at build time.
src/routeTree.gen.ts

# Local env / editor noise
.env
.env.local
*.local
.DS_Store
